static
String
cleanse_sql
String $text;[,Boolean $strip_blank=false]
This static method allows you to remove all JavaScript comments.
For inline comments, they begin with "//".
For comments blocks, they begin with "/*" and finish with "*/".
If $strip_blank is set to true, that will remove all Linefeed, Cariage Return and tabulation.